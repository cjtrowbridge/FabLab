{
  "schema_version": 1,
  "phase": "phase_4_skeleton",
  "required_commands": ["git", "python3"],
  "downloads_default": "disabled",
  "notes": [
    "This skeleton performs local checks only.",
    "Provider setup/download behavior must be added through explicit future plans."
  ]
}
