# step.parts

step.parts is a component source provider for off-the-shelf STEP parts.

Component models from step.parts require provenance, measurement confidence, fit-risk classification, and interface validation before dependent fabricated parts can be called fabrication-ready.
